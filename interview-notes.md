# 面试素材本

> **用法**:每节课结束后,把"踩过的坑 / 做过的权衡 / 形成的判断"随手记进来。
> 面试答得好不好,差别不在背了多少概念,而在**有没有真实经历 + 归因 + 形成的判断**。
> 本文件按「面试官会怎么问」组织,而不是按课程顺序。
>
> 状态:已沉淀 L1-L5(阶段一 + L5)的素材。**每封版一节课就回来补一次。**

---

## 一、最有杀伤力的三条(优先背熟)

### 1. "你怎么保证 agent 真的执行了操作,而不是嘴上说说?"
**答:看 `tool_calls`,不看模型的话术。**

> 我做长期记忆时踩过一次:让模型在用户透露偏好时调 `remember` 工具落盘。跑完模型回我
> "我已经记下了您的姓名和快递偏好😊",看起来完全正常。但我去查持久化文件,**根本不存在**——
> `remember` 从头到尾没被调用过。模型只是**生成了一句像模像样的话,并没有执行动作**。
>
> 我是这样定位的:先查物证(文件在不在),再在工具执行处打印真实的 `tool_calls`,眼见为实。
> 根因是 **prompt 太软**——我原来只写"必要时可以记住用户偏好"。改成强命令、并**直接点名禁掉
> 偷懒话术**("绝对不要只嘴上说'已记住'却不调用工具"),模型就乖乖调了。
>
> 这件事给我一个判断:**agent 的验收必须基于可观测的动作,不能基于模型的自述。**
> 所以我做可观测性时,第一优先就是把每次工具调用完整记下来。

**加分延伸**:这直接引出为什么 agent 需要 tracing/日志,而不只是看最终回复。

---

### 2. "你怎么做成本 / 延迟优化?"
**答:分任务挑模型 + 只暴露该暴露的工具。**

> 两个我实际做过的:
>
> **(a) 分任务挑模型。** 我的客服 agent 里,主对话和意图路由用的是不同模型。路由只需要输出
> 一个类别词,我就用快而便宜的普通模型。**这里踩过一个坑**:一开始路由用了推理型模型,
> `max_tokens` 设成 5,结果思维链把 token 预算吃光,`content` 返回空字符串,路由静默失灵、
> 全部兜底到默认分组。换成非推理模型就好了。结论:**分类/路由这种要快要稳的活,
> 不该用会"先想一会儿"的模型。**
>
> **(b) 工具分组路由,不把全部工具塞给模型。** 工具一多有两个问题:①模型选择准确率下降
> ②**每一轮请求都要重复携带全部工具 schema**,既烧 token 又挤占上下文。我的做法是先用代码
> 判断用户意图落在哪个场景,本轮只暴露那一组工具。

---

### 3. "你用过 LangChain 之类的框架吗?"(**学完 L9 后再用这条**)
**答:用过,而且我是先手写过 agent loop 才去用框架的。**

> 我是从零手写 agent loop 起步的:自己实现 while 循环、解析 `tool_calls`、本地执行工具、
> 把结果以 `role: tool` 回填 messages、再喂回模型。所以我很清楚框架里
> AgentExecutor 那一层到底替我做了什么、哪些是它的抽象、哪些是模型本身的能力。
>
> 这让我在用框架时不至于"只会调 API"——比如出问题时我知道该去看哪一层。

**注**:这是**强加分回答**,但必须等 L9 真的用 LangChain 重写过项目再讲,别提前吹。

---

## 二、核心概念的"我怎么理解"(用自己的话,别背定义)

### Agent 的能力从哪来?
**"能力来自工具,不来自 loop;可靠性来自约束。"**
- loop 本身很薄——就是"问模型 → 它要调工具就执行 → 结果回填 → 再问"。
- **链式调用(多步任务)是 loop 涌现出来的,不需要额外代码**:模型第一轮查订单,
  第二轮看到结果自己决定要不要退款。我当时零新增代码就支持了任意长的工具链。
- 但工具一多就得加约束(分组路由),否则选择准确率和 token 成本都崩。

### LLM 无状态,agent 怎么"记住"?
**没有魔法,就是"存下来 + 下次读出来拼进 prompt"。**
- **短期记忆**:一次会话内累积的 messages 列表。进程一死就没。
- **长期记忆**:自己实现"存 + 取"闭环——识别到值得记的事实写进持久层(文件/DB/向量库),
  新会话启动时读出来**注入 system prompt**。
- **注入要放 `system` 角色**,不能伪装成 user 消息:模型被训练成把 system 当作贯穿整段对话的
  背景设定;伪装成 user 容易被理解成"用户刚刚说了这句",多轮里串味。

### 记忆 vs 状态(容易被追问的区分)
**记忆是给模型看的,状态是给代码用的。**
- 我的防重复退款用一个 `REFUNDED` 集合,靠 `if order_id in REFUNDED` 判断——
  这份数据**从没进过 messages**,所以它是状态不是记忆。

### 错误处理:"错误也是信息"
工具抛异常不该让 agent 崩。用 try/except 接住,把错误文本作为 tool 结果**回传给模型**,
模型会自己调整策略:换参数重试、改调别的工具、或体面地告诉用户。
**你只需保证错误能以文字到达它眼前,自愈是白送的。**

---

## 三、踩坑清单(被问"遇到过什么难搞的 bug"时用)

### 1. 静默兜底掩盖 bug(最值得讲)
LLM 路由写了 `return label if label in VALID_GROUPS else "order"` 这样的校验兜底。
结果所有输入都被归到 `order`,但**程序不报错**——因为兜底把异常吞了。
定位方法:**在兜底之前把真实值用 `repr`/`!r` 打出来**,立刻看到 `原始=''`。

> **教训:fallback 是双刃剑——让程序不崩,也把 bug 藏起来。写兜底的地方,
> 调试期一定要能看见"兜底之前的真实值"。**

### 2. 漏写兜底 return → 静默返回 None
函数只在 `if` 分支里 return,忘了另一支,Python **不报错不警告,默默返回 `None`**,
然后在很远的地方以奇怪方式炸掉(我这次炸在 API 报 "content should be a string")。
> **自查动作:写完带返回值的函数,回头扫一眼"每个分支都 return 了吗"。**

### 3. messages 混血列表导致 AttributeError
`messages` 里同时躺着两种东西:自己手写的 system/user/tool 是 **dict**(`m["role"]`),
API 返回后 append 的 assistant 是 **SDK 对象**(`m.role`)。统一用一种方式访问必炸。
> 看到 `'dict' object has no attribute X` 或 `'X' object is not subscriptable`,
> 先想"我这个列表是不是混血了"。

### 4. 环境变量:VSCode 的环境快照坑
Windows 上用 `[Environment]::SetEnvironmentVariable(...,"User")` 设的永久变量,
**VSCode 读不到**——因为它在启动那一刻就把环境变量拍了快照,开新终端标签也没用,
**必须完全重启 VSCode**。

### 5. `ensure_ascii=False`
中文 JSON 不加这个参数会被转义成 `\uXXXX`。除了 debug 时看不懂,更实际的问题是:
**这些 JSON 要作为工具结果回传给模型,转义后平白多烧 token**(一个中文字可能从 1-2 token
变成 4-5 token)。

---

## 四、能讲的项目(简历素材)

### 命令行多工具客服 Agent(阶段一 capstone)
- 手写 agent loop,不依赖框架
- 5 个业务工具(查订单/退款/查退款进度/改地址/查积分),按场景**分组路由**
- **共享底座 + 场景增量**的分组策略:通用查询工具在每组都带上,专用工具按意图追加
- 会话内多轮记忆(能理解"把**它**退了"里的指代)
- 错误自愈:工具异常转成错误 JSON 回传,模型自行调整
- 循环次数上限兜底,防止模型抽风空转
- 路由做了两版对比:**关键词匹配**(零成本但硬编码) vs **LLM 分类**(灵活但每轮多一次调用),
  能讲清两者的取舍

### 跨会话长期记忆
- `remember` 工具 + JSON 持久层 + system prompt 注入,实现跨进程重启的用户记忆
- 设计取舍:**把"要不要记"折进 tool use 让模型自己判断**,而不是另开一次分类调用
  (省一次 LLM 调用)

---

## 五、一个容易被忽略的加分项
**"熟练使用 AI 编程工具(Cursor / Claude Code)、具备 AI 原生思维"** 被拼多多写进硬性要求、
沃孚写进核心门槛。

我**全程用 Claude Code 学习和开发 agent**——这本身就是素材,可以讲:
- 用 agent 工具来学 agent 开发,对模型行为有第一手体感
- 对"模型什么时候靠谱、什么时候会演"有实际判断力(见第一节那条)

---

## 六、L6:上下文管理 + Prompt/结构化输出(新增)

### "多轮对话成本为什么会失控?" —— 平方级
无状态 API 每轮重发全部历史 → 累计 token = 1+2+…+N ≈ N²/2。**单次请求线性,整段会话累计平方。**
优化手段:阈值触发的上下文压缩(混合策略:最近 N 条原文 + 更早的摘要)。

### "上下文越长越好吗?" —— 不,窗口大≠该塞满
"lost in the middle":长上下文里模型对首尾利用好、中间易忽略。塞满 128K 未必胜过塞 10 条精华。

### "你怎么做上下文压缩?" —— 有几个坑我踩过
1. **阈值必须高于"压缩地板价"**(system+schema+摘要+保留原文),否则压完还超 → 每轮都压(thrashing)。
2. **反复摘要会衰减信息**(复印件的复印件):我亲眼看到用户名在第二轮再压时丢了。
3. 所以**稳定高价值事实(姓名/偏好/ID)该走结构化长期记忆(remember),不该指望有损摘要**;
   摘要只负责压"聊了啥"。—— 这是记忆(L5)与上下文(L6)的分工。

### "怎么让 agent 代码可测?" —— 纯逻辑与副作用分离 + 依赖注入
把压缩逻辑做成不碰 LLM/网络的纯模块,摘要器**依赖注入**进来;测试时注入假摘要器 →
测试快/稳/免费/能测异常路径。对照蔚蓝 JD"评估与回归测试体系"。
配套:①`response.usage.prompt_tokens` 而非自己估 token(自估会漏算工具 schema)
②"假绿":没断言的测试永远通过,TDD 先让它红、变异测试防它。

### 结构化输出(输出控制)
三层次:prompt 请求(无保证)< JSON mode(保证合法 JSON)< schema 强制(字段全保证)。
**Function Calling 本质就是结构化输出。** 选型:触发动作→FC;数据给代码消费→结构化输出;给人看→纯文本。

### Prompt 工程一句话
别用模糊限定词、点名禁掉已知失败模式、prompt 每轮重发也占预算(简洁而精确)。
**信息在上下文 ≠ 模型会用它**:问法和 system prompt 是否允许都影响它愿不愿意说。

### 一条基础设施纪律
模型可用性/名字/额度**以当前端点返回为准**,别信文档或"上次能用"(`client.models.list()` 查);
400 报错会并排给出合法值与你传的值,逐字符比(我栽过 `-` vs `_`)。

---

## 七、L7:RAG 与向量数据库(新增,JD 8/10 面试必问)

### "讲讲 RAG 是怎么工作的?"
两个阶段:①离线建索引(文档→切块→embedding→存向量库)②在线检索生成(问题 embedding→
向量库找 top-k→塞 prompt→grounding 生成)。本质:让 LLM 回答前先"查资料",开卷考试。

### "embedding 为什么能做语义检索?关键词匹配不行吗?"
embedding 把文本变成向量,语义相近→向量相近。"马铃薯 vs 土豆"字面零重叠但语义相同,
关键词匹配漏,embedding 命中。这是 RAG 能"模糊匹配"的根基。

### "向量数据库解决什么?"(别混三个部件)
**向量库负责"找得快"**(海量向量里毫秒找 top-k,HNSW 把 O(N)→~O(logN));
top-k 负责"塞得少"(控成本+抗干扰);LLM 负责"答得好"。三件事别混。

### "你用什么向量库,为什么?"(选型话术)
Chroma:嵌入式/零配置/HNSW/sqlite 持久化,适合中小规模+快速迭代。
上千万级或高并发→Milvus/Qdrant;数据在 Postgres→pgvector;只要极致速度→FAISS。
底层都用 ANN 索引。—— 展示懂 landscape,不是只会一个工具。

### "RAG 效果不好怎么排查?"(本节亲手验证,王牌回答)
**先查 chunking**。我实测过:同一份 embedding+向量库+prompt,只把切法从固定字符窗口
换成按语义边界(标题)切,检索和答案质量都上一个台阶——固定窗口会切碎句子、跨段落,
检索到的块本身就不干净,模型自然答不利索。生产用 RecursiveCharacterTextSplitter。
chunk_size / overlap 也是旋钮:overlap 防关键信息被切在块边界丢失。

### "RAG 还能怎么优化?"(埋点,显深度)
rerank 重排(粗召回→cross-encoder 精排)、hybrid 检索(向量+BM25)、query rewrite、
metadata 过滤、RAG 评估(recall@k/忠实度)、embedding 缓存+重试。
大知识库:embedding 分批(单次~2048 上限)、id 用内容哈希做增量更新去重(不重复嵌)。

### "怎么防止 RAG 幻觉?"
grounding:强 prompt 约束"只依据资料回答,没有就说暂无,不得编造";
第二道防线可用检索 distance 阈值,太远就判无相关、不硬塞给 LLM。

### "你怎么评估 RAG / 保证改动不弄坏已有功能?"(亲手做过,王牌回答)
我给 RAG 建了**评估集**(18 条),分四区:正常可答 / 库里没有 / 越权 / 对抗(诱导、假前提、
prompt 注入),每条带 `must_not_contain` 硬红线(防泄露 system prompt、防确认假前提、防越权执行)。
打分卡按**两条正交指标**统计:**检索命中**与**答案正确**——
> 检索对但答案错 = grounding 问题;检索就没命中 = chunking/embedding 问题。分开看才知道修哪。

**它当场帮我抓到过一次真实回归**:我改 grounding prompt 去修"退款能秒到账吗"这条,
结果另一条"支持100天退货对吧"从"正确纠正"退化成了"拒答"——**只看我在修的那条根本发现不了**。
根因是 prompt 的分支按"具体说法"划了,改成**按"资料是否涉及该主题"划**后两条都对,
且拒答类没被带偏。这就是"改一处→跑全套→确认净改进"的回归循环。

### "关键词匹配评分够用吗?"(踩过三次的坑)
不够。我连续三次被同一类假阴性坑:`"7 天"vs"7天"`(空格)、`"而非100天"`、`"并非秒到"`——
**子串匹配无法区分"确认"和"否认"**,正确纠正的答案被判成违规。
所以评分要么把红线写成只抓确认措辞(治标),要么升级 **LLM-as-judge**(治本,但有自己的坑:
裁判偏好、不稳定、成本)。**教训:评估集也是代码,也有 bug,烂指标会对你撒谎——
盲信它你会去"修"一个没坏的系统。**

### 工程:依赖管理用 uv(可写进简历)
uv init/add/sync;pyproject.toml + uv.lock 入库,双机一键复现。比 conda 现代、快。

---

## 八、L8:路由 / ReAct / Plan-and-Execute(新增,JD 6/10)

### "讲讲 ReAct?"——先说它不是新东西
现代 **function-calling agent loop 本质就是 ReAct 的协议化版本**:Thought(模型推理)→
Action(结构化 `tool_calls` 字段)→ Observation(`role="tool"` 消息)→ 回到 Thought。
原论文年代(GPT-3)没有 function calling,ReAct 是纯 prompt 格式、靠正则抠 `Action: query_order[A123]`;
今天 API 把它协议化了。**文本版还有用**:模型不支持 tool use 时(小模型/老开源模型)的降级方案。

### "ReAct 和 Plan-and-Execute 怎么选?"——它们是一条轴的两端
**不是两种架构,是"重规划频率"这条连续轴的两端**:ReAct 每步重规划 ↔ P&E 只规划一次,
真实系统落中间,所以主流实现都是 **Plan → Execute → 偏差触发 Replan**。
- 失败模式:每步能改主意→绕圈/无限循环;从不改主意→**机械执行过时计划**。
- **反直觉点:P&E 不比 ReAct"更可控",它只是把不确定性提前了**——赌注一次性押在"规划期信息是否足够"。

### "客服 agent 该用哪种?"(王牌:自主性是成本不是收益)
> **自主性是成本,不是收益。只在"环境不可预测 + 试错便宜"两条同时成立时才买它。**
> coding agent(Claude Code)两条都满足→纯 ReAct(它的 plan mode 是给人审批的工件,不是调度器);
> 客服两条都不成立(意图有界、合规硬约束、延迟敏感、错退款代价大)→ **静态路由收敛意图 + 分支内跑受限短 loop**。

### "你实测过两种范式吗?"(王牌:亲手跑 + 三个打脸)
我在三类任务上真跑对比,**ReAct 三战全胜或打平**:
- **单步任务**:P&E 调用数更少但延迟打平——因为它的规划走慢模型,**调用数少≠更快,要看每次调用成本**。
- **并行扇出任务**:P&E **幻觉翻车**——编出不存在的订单号。深层原因:**扇出目标要运行时才发现
  (先 list 才知道有哪些单),而 P&E 规划期就得定死,时序天生冲突**。独立可并行还不够,目标必须规划期已知。
- **观察分支任务**:P&E 靠 replan 自救但慢 4 倍;ReAct 原生处理(看到观察再决定)。
> **最值钱的一条:光看 token/调用数会把"失败"读成"高效"**——P&E 最省那次(831 token)其实啥也没干、
> 直接幻觉。所以 correct 列必须人工判,**这就是 L12 要上 LLM-as-judge 的动机**(第三次撞上子串/数字判不了对错)。

### "生产级 agent loop 要有什么?"(工程硬功)
玩具 `while True` 上不了生产。至少要:
1. **终止语义显式枚举**(不返回字符串):`FINAL_ANSWER/HANDOFF` + 被迫的 `MAX_STEPS/NO_PROGRESS/BUDGET_EXCEEDED/TOOL_FATAL`。
   为什么枚举→**可观测(trace 字段)、可测试(pytest 断言它)、上层能分情况兜底**。
2. **纯逻辑/副作用分离**:终止判断、no-progress、预算记账全是不碰网络的纯函数→能 pytest;碰 API 的只做接线。
3. **每轮一行结构化 JSON 日志**(给机器采,算 P95 延迟/token 成本/工具分布),用 logger 不用 print。
4. **循环上限不是防模型犯傻,是防钱包破产**:失控 loop + 长上下文 = 平方级 token。

### "工具执行层怎么区分模型错和系统错?"(细节分)
两级:**调用前**用 `inspect.signature().bind()` 校验参数,把"模型传错参数名"挡在门外并回传 expected 让它自愈;
**调用后**的异常按三类分流——业务错(原文回传自愈)、致命错(**内部记详细日志、外部只回脱敏话术**并终止)、
瞬时错(该代码重试)。硬约束:任何路径都返回字符串(每个 tool_call_id 必须有且仅有一条回复)。

### "stop_reason 该怎么设计?"(可观测性判据)
**stop_reason 要如实说出"哪道闸门拦住了你"**。判据:根因和排查方向同一类才可复用,否则新加成员。
- "计划有环"和"运行时转圈"不同类(验证期 0 步 vs 运行期多步)→ 新加 `INVALID_PLAN`,别复用 `NO_PROGRESS`。
- "重规划额度耗尽"和"步数上限"同类(都是计数放弃)→ 可复用 `MAX_STEPS`(或新加 `REPLAN_EXHAUSTED`)。
- 但两者都≠ `BUDGET_EXCEEDED`(金钱闸)——**计数闸和金钱闸别混报**。

### "json_schema 端点不支持怎么办?"(生产战况)
DeepSeek 端点对 `response_format: json_schema` 返回 400(`This response_format type is unavailable now`),
**6 次规划调用全部降级** `json_object` + 把字段要求写进 prompt + 自己校验。**graceful degradation**:
json_schema 失败退 json_object,解析也失败就退 ReAct——每层有下层兜着,系统不会因一环挂掉就整个崩。

### 度量踩坑(项目细节,显严谨)
对比实验最初用 `result.steps` 当 LLM 调用数,但 P&E 的 steps 数的是**工具执行数**不是模型调用数,
谎报"P&E 省一半"。**教训:对比实验里度量口径错一个,结论全废**。修法:加独立 `llm_calls` 字段各自如实计数。

---

## 锚点 L8 · smolagents:动作表示法两范式（CodeAct vs JSON tool-calling）
- **两范式**:JSON tool-calling（模型返回 name+JSON，动作**有界**，靠"能力清单"约束——没给的做不了）
  vs **CodeAct**（模型直接写 Python 扔沙箱跑，动作**近乎无界**，靠"沙箱"约束）。
- **CodeAct 省在哪**:把"受益于循环/组合的中间步"从 **LLM 往返**下沉到**一次代码执行**——模型被唤醒一次去*写*循环，
  而非 N 次去*驱动*循环（扇出算例:`sum(o['amount']*rate for o in get_orders())` 一步完 vs N+ 次往返）。
- **代价三层**:①安全——攻击面从有界变无界（注入可写 `os.system`），需受限解释器/Docker 沙箱;
  ②弱模型崩坏——当场写对能跑的代码 >> 填 name+JSON;③可控性退化——动作是不透明代码块，
  调用前无法 bind 校验、异常统一粗粒度上报、审计要 parse 代码。**CodeAct 用可控性换表达力**。
- **选型判据**:合规/逐笔审计/高危二次确认的**客服** → JSON tool-calling;自主长执行/代码组合受益的**编码** → CodeAct。
- **规划轴**:纯 ReAct ↔ 周期性重规划（smolagents，更新时"忘掉旧计划只喂真实观察"防锚死）↔ Plan-and-Execute（一次定死，扇出易翻）。
  为何 pi/Claude Code 不内置 planner 而客服要显式路由:自主长执行+环境实时变 vs 短对话+合规+要可预测。

## 客服 Agent 设计面高频追问（阶段二 capstone 模拟面试沉淀）
- **澄清模糊需求三线索**:边界（做什么/不做什么）· 真值（答案从哪来/准不准）· 风险（谁授权/怎么兜底/怎么衡量）。任何 agent 需求都能问穿。
- **路由权归属是两层，别合并**:意图路由用**模型**（LLM-as-router + 结构化输出，灵活），高危执行用**代码**（阈值 hard-code，可靠）。
  "因为退款敏感所以全代码编排"是典型错——纯代码/关键词路由处理不了"我上周买的鞋想弄一下"这种无关键词自然语句。
- **质量指标必须拆两个正交项**:自动化率（deflection，扛住了）+ 事实准确率/幻觉率（对不对，**不能模型自评**，要人工抽检/LLM-judge）。
- **RAG 挡幻觉**:向量检索**永远返回最近 chunk**（哪怕没覆盖）→ 用**相似度阈值**判"没覆盖"转人工，**阈值从评估集标定**（precision/recall 权衡）。
- **上下文杀手是 RAG 检索载荷不是对话轮数**:控法=chunk 切准 + 压 top-k;**政策原文绝不 summarize**（会丢"7天无理由"精确措辞），会话历史可 summarize。
- **越权校验放工具层**:`query_order` 强制 `where user_id=session.user.id`，且 `user_id` **由代码从登录会话注入，绝不接受模型输出的 id**。
- **安全阈值 hard-code 不让 LLM 判**:金额超 200 即使模型觉得合理也转人工（可靠性来自代码约束不来自模型话术）。事故难点在**检测**（不投诉就不知道悄悄退错款）→ 要审计日志。

### 动手实现沉淀（capstone 第三轮 build，比设计面更锋利、追一层才问得出）
- **归属校验要「内部分开、对外合并」**:`forbidden`(存在但非本人) 和 `not_found`(不存在) 在**代码/审计日志里必须分开**——有人反复撞别人订单号=攻击信号，要能检测;但**对用户话术统一收敛**成「没查到这笔订单」。
  为什么:两种回话有别 = **枚举预言机(enumeration oracle / IDOR)**,坏人拿一堆随机订单号就能筛出哪些真实存在。**审计要区分（检测攻击），话术要统一（不泄露存在性）。**
- **高危参数注入防线有两道，防的东西不同**:①工具 schema **不暴露** `user_id`/`amount` → 让规矩模型**根本不会去给**（防无心）;②dispatch/工具层**绝不从模型 args 取**这些值，只认代码注入的会话值/订单库真值（防有意/防注入）。
  一句话:**高危决策依赖的关键数据，来源必须锁在代码侧**——身份锁会话、金额锁订单库。实测:模型 args 里偷塞 `user_id=u_li` 想撬别人订单 → 被第二道忽略，仍 `forbidden`。
- **"阈值为什么用代码 if 不用 prompt 提示"满分答法**(动钱不可逆操作的核心卖点):
  ① prompt=软约束(概率采样),四种破防——社工/注入("经理同意了")、长上下文指令漂移(lost in the middle)、便宜小模型遵从差、temperature 抖动;
  ② 代码 `if amount>200`=硬约束,跟用户说啥/上下文多长/哪个模型/注入啥**全无关**,100% 成立;
  ③ **压死骆驼的稻草=可测试性**:你**写不出测试证明"prompt 一定守住 200"**(概率的),但能写 pytest 证明 `if` 永远拦——**安全边界要放在能被测试、能被审计的地方，不能放在能被话术绕过的地方**;
  ④ 退款是不可逆写,一次破防=真金白银+欺诈入口,不接受"通常正确",只接受"保证",而保证只有代码给得了。
- **多跳复用而非重写安全逻辑**:`process_refund` 第一步直接调 `query_order` 拿「归属校验+订单真实金额」,不重复造安全代码(面试官爱看)。归属闸和金额阈值闸**各管一层**——自己的订单超阈值照样退不了。
- **单元测试三条硬道理（capstone 亲手写一套，含 fixture）**:
  ① **空测试/没 assert 的测试永远绿 = 最坏的假绿**,比没测试更危险(让你以为有覆盖);信一个测试前先 **red-green**——故意写错让它红一次,确认它真会咬人,再改绿。
  ② **测试隔离**:被测函数若写全局状态(如退款账本 `_REFUNDS`),测试间会互相污染 → 产生**执行顺序依赖 = flaky test**;用 pytest **`autouse` fixture** 每个测试前后清状态,保证每条独立。
  ③ **先测纯逻辑层**(tools:同输入同输出,快又稳),碰网络的 loop 用 **test double**(注入 FakeClient,把"模型怎么答"变成自己说了算,才能稳定复现 MAX_STEPS 等罕见路径)——这也是 client/guard 走参数注入的真正原因:**为了可测**。
- **"接一个工具进 agent 要动哪几处"(RAG 政策工具接线实战)**:一个工具从"单文件能跑"到"agent 能用"要**三处同步改**,少一处坏法不同——① `TOOLS` schema 不加→模型**不知道有这工具**、永不调;② `dispatch` 路由不加→模型调了但**返回 unknown_tool**;③ `SYSTEM_PROMPT` 的 reason 话术(如 `not_covered`)不加→工具返回了但模型**不知道怎么回**用户。**这是"改一半"在架构层的样子**——三处是一个动作的三个部分。
- **"你的客服系统怎么防越权/该不该注入 user_id"满分判据**:判据不是"数据敏不敏感",而是**"这条数据是否按用户隔离"**——`query_order`/`process_refund` 是**私有数据(每笔订单有主)**→ 工具层强制注入会话 `user_id` 上锁;`search_policy` 是**共享知识(同一份政策对所有人一样、无主)**→ 不需要归属校验。**有主的锁,无主的不锁。**
- **"你代码怎么做到不联网就能测"(mock / 可测性深水区,一套四连招)**:
  ① **import 时不能有 I/O**:原来 `_COLLECTION = _build_index()` 在 import 那刻就打 OpenAI 建索引 → 测试文件一 `import` 就依赖真实网络+key,**连收集都收不了**。改成惰性 `_get_collection()`(首次调用才建)→ import 无副作用,才能被测。**"让代码可测"的核心功是消除 import 期副作用。**
  ② **测试替身/mock**:单元测试只测**你的逻辑**(阈值分支),不测 OpenAI/DeepSeek 的质量(不是你的代码、慢、花钱、网络会抖=假红)。手法=把三个接缝(`_embed`/`_get_collection`/`_grounded_answer`)换成手捏假货,喂**你指定的距离**,只看 if 走对没。
  ③ **`monkeypatch`(pytest 原生)**:测试期临时替换、**测完自动还原**(手动重赋值忘还原会污染别的测试,同 `_REFUNDS` 不清会串味)。
  ④ **绿 ≠ 对 + 行为验证**:漏 patch 的测试照样绿,但它偷打真网络(实测该条独吃 3s,其余毫秒级)——照妖镜是**"拔了网线还能过吗?"**;进阶=**行为验证**:把 `_grounded_answer` 换成"一被调用就 raise"的 `_boom`,断言 not_covered 时**不该发生的生成调用没发生**(省钱),比只断言返回值高一档。
- **"RAG 距离阈值你怎么定的"满分答法(标定实战,王牌)**:不是拍脑袋——用**评估集的距离分布**标定。
  ① **少量点会把阈值定错**:sit1 我用 4 个探针拍了 0.9 看着挺稳;换成 eval 13 条(9 normal + 4 unanswerable)重标,发现 normal 有条**长尾拖到 1.09**(生鲜退货/跨款换货这类边界正例),4 个点根本没采到 → **0.9 会把 0.914/1.041/1.090 三条正经问题误踢转人工**。教训:**阈值要从分布标定,不能从几个样本猜,否则漏掉长尾。**
  ② **沟里怎么放**:normal 0.78–1.09 / unanswerable 1.34–1.52,沟(1.09,1.34)取 **1.15 偏严侧**——理由是**客服场景幻觉泄漏 > 过度转人工**(绕路可接受,答错不可接受),但**不贴沟**(留正例长尾余量)。
  ③ **阈值是运营旋钮不是常量**:上线后拿真实误判率回调(误判正例多→上调),有 `eval/` 回归网兜底。这句直接引出 L12/L13 可观测性。
  ④ **距离闸的边界**:它只挡"没覆盖",挡不了"覆盖了但被诱导"(adversarial 假前提如"100天无理由对吧"距离很小,靠 grounding 生成纠正,不靠距离);out_of_scope(越权查/改政策)靠工具层权限,也不靠距离。**一层挡一类,别指望距离闸包打天下。**
- **(设计谈资·置信度分档路由,能追一层)**:二档(覆盖/转人工)太糙会误伤边界问题;生产做法是**三档 confidence-band**——`distance<t_lo`直接答 / `t_lo~t_hi`(就是那条"沟")**反问澄清让用户 rephrase→重检索** / `>t_hi`转人工。中间档用"改进 query 再检索"挽回,既压幻觉又降转人工率(1 轮澄清 << 1 次人工)。变体"总结 top-1 反问'您是想问退款政策吗'"要防**假前提诱导 + 隐私**(政策内容无主、风险低于用户私有数据)。**junior 能答阈值,senior 能设计降级路径**——留作 capstone 可选增强 / LangGraph 版用 conditional edge 实现的对比素材。
- **"转人工怎么做的"满分答法(escalate 做成终态工具,不靠 prompt 话术)**:三个生产事故点决定它必须是**真工具**——
  ① **必须开审计工单**:光让模型说"已转接"→系统零记录,人工那头收不到,且**事故靠检测**(不投诉就不知道悄悄办错),要落结构化台账;
  ② **必须真终止 loop**:说完转人工 loop 还在转,下一轮模型可能改口/又承诺退款 → 工具返回带 **`terminal:True`** 信号,`run()` 每轮 dispatch 后**通用地** `result.get("terminal")` 判真→直接 return 转人工话术(带工单号)、退出 loop。**通用读信号、不 hardcode 工具名**(L8"闸门如实报、loop 读信号"回扣;以后加终态工具不用改 loop);
  ③ **安全动作不建在"模型这次记得说"上**:跟退款阈值同理,建在**代码里可审计、可测试**处。测试查物证(台账真落账 + 工单连号),不只信返回值。
- **Python 基本功·`is` vs `==`(踩过,ruff F632)**:`is`比**身份**(同一对象)、`==`比**值**。运行时 f-string 拼出的字符串是**新对象**,`ticket_id is "T0001"`会 False(偶尔字符串驻留侥幸相等→时灵时不灵最坑)。**铁律:`is` 只用于 `None`/`True`/`False` 单例,比值一律 `==`**。反讽:同一行 `terminal is True` 能过(True 是单例),`ticket_id is "T0001"` 挂——正好演示为什么 is 只配单例。

## 九、L9:框架对比(LangGraph vs 裸 SDK)+ ADR(新增,JD 6/10 简历关键词)

- **框架四象限(能答"优劣差异",百度 JD 全部点名)**:LangGraph=有状态图/强控制流(有界可靠场景);LangChain=组件链/快速拼 demo;LlamaIndex=RAG 重;AutoGen=多 agent 对话;CrewAI=角色扮演多 agent。**判据:有界要可靠→LangGraph;重检索→LlamaIndex;多 agent 头脑风暴→AutoGen/CrewAI。**
- **"我把裸 SDK loop 用 LangGraph 重写了一版"(有对照才有观点)**:while 循环→图(StateGraph);messages→State;调 LLM→agent 节点;dispatch→tools 节点;`if not tool_calls`→条件边;回到 while 顶→回边;terminal 检查→tools 后第二条件边;LoopGuard→recursion_limit。**框架没发明新概念,给循环一个标准形状。**
- **ADR 结论=编排层用裸 SDK,三条硬理由(面试怼"为什么不用框架"的答案)**:① 框架招牌价值(可视化/checkpoint/易分支)对有界短 loop **用不上**;② 安全注入(`user_id`)逼我写自定义节点,内置 `ToolNode`/`create_react_agent` 不合身——**框架连省代码都没做到**;③ 省依赖只是附带。**触发切 LangGraph:多分支(三档澄清)/human-in-loop/多 agent/checkpoint。**
- **三条框架金句**:① **框架工具价值 ∝ 你把结构静态声明的程度**(条件边不给 path_map,能跑但画不出图/校验不了);② **优雅捷径假设你走 happy path**,非标准安全需求就不合身;③ `recursion_limit` 是 raise 不是优雅 return,要自己 try/except 兜底(框架给闸门,默认行为未必是你要的)。
- 迁移摩擦:`tool_calls` 裸 SDK 是嵌套对象(`call.function.arguments` 是 JSON 字符串)、LangChain 是扁平 dict(`call["args"]` 已解析)。

## 十、L10:MCP 与工具生态(新增,JD 4/10 但 2025-26 热点必问)

- **"讲讲 MCP"**:Anthropic 2024 末的开放标准,给 AI 工具生态定统一"插座"。**client-server**:server 用 `@mcp.tool` 暴露工具(docstring→description、类型注解→schema),client 靠 `list_tools` **发现**、`call_tool` **调用**,stdio(本地)/Streamable HTTP(远程)传输。**三原语:工具(可执行)/资源(只读数据)/提示模板。** 价值=一次开发处处可用。
- **⭐"你怎么保证 MCP 工具安全"(踩坑+正解,王牌)**:包成 MCP 后工具参数全来自 client,**我原本 session 注入的身份防线失效**——被注入的模型能传任意 `user_id` 越权。正解:**身份属 server 的认证上下文,不做成 client 可填的工具参数**;工具只暴露 `order_id`,`user_id` 由 server 注入。实测 client 硬塞 `user_id` 冒充,server 无视仍 forbidden——**防线在 server 逻辑(传了也不信),不在参数校验。** 原则同 capstone:高危数据来源锁在被调用方。
- **MCP 安全四风险**:① 工具描述投毒(description 进上下文=prompt 注入变种,每次会话生效);② 恶意/被劫持 server(供应链);③ 同名工具遮蔽(tool shadowing,截走敏感参数);④ 凭证滥用。缓解:审查描述(当不可信输入)/锁版本/最小权限凭证。
- **工具描述的艺术(ACI 原则,反哺了 capstone)**:① **"什么时候用" > "能做什么"**;② **边界/反例最重要**(调用失败头号原因是模型不知道工具**不能**做什么);③ 参数用具体例子(附 1-5 调用示例,准确率 72%→90%);④ **参数保真性**(工具静默改写输入=模型 debug 不出的隐蔽 bug,模型感知的世界=工具操作的世界);⑤ **回答话术留 system prompt、不进工具描述**(否则工具没法复用,尤其 MCP 要一次开发处处可用)。我把四个工具统一成 `功能/时机/边界/参数/返回` 结构。
- **主动工具发现(L3 伏笔收口)**:工具上百个不能全量注入 schema。① 主动发现(Agent 声明缺口→工具搜索元工具→语义匹配注入,MCP-Zero 2800 工具省 ~98% token;Anthropic Tool Search 让 Opus4 准确率 49%→74%);② Skills 渐进披露(只给薄目录、按需读文件,工具选择变知识检索,不用向量索引)。工程:**动态加载别破坏 KV Cache**——新 schema 追加末尾、保前缀稳定(L6 注入位置回扣)。**判断何时用:几个工具全塞 prompt 就好,上百工具才值这套基础设施(senior vs junior)。**

## 十一、Reflection 反思范式(新增,JD1/3 点名「Planning-Acting-Reflection 闭环」)

- **"你的 agent 有反思能力吗 / 怎么做 Planning-Acting-Reflection 闭环"**:我给客服 capstone 加了 **reflect 节点**——答复**发出前**用 LLM-as-judge 按红线自审,命中就重写一次。之前只有 Planning(模型选工具)-Acting(执行)-Observing(结果回喂),**缺最后的 Reflection**;补上才闭环。
- **"反思"要能拆三层(区分度)**:① **Self-Refine**(Madaan 2023)=单次输出 生成→批判→改写,不涉任务成败、不存 memory;② **Reflexion**(Shinn 2023)=任务**失败后**写「为什么失败」**存进 memory**、带教训重试,是**语言化的 RL**(不更新权重,更新 context 经验);③ **Reflection node**=图里加 critic 节点+条件边(LangGraph pattern)。我做的是 ①+③;②那种跨 attempt 存教训更重(接书 Ch8 持续进化),讲得清但没做。
- **⭐反直觉判断(王牌)**:反思**只审「代码判不了、但有对错」的话术层**(泄露/越权/编造/答非所问)。**能 hard-code 的(越权/超额)继续 hard-code,别改成让 LLM 反思**——可靠性来自约束,用概率手段守确定性红线是**倒退**。三道 hardcode 安全线在上游守红线,reflect 在下游守话术,分工明确。
- **Evaluator 信号来源越靠上越可靠**:代码/规则(零成本最可靠)> 真实环境反馈(测试过没过)> LLM-as-judge(灵活但有盲区)。
- **⭐fail-open 判断(踩坑+正解)**:reflect 是叠在**已执行的**安全线之上的**质量层**——自身故障(模型宕机/非法 JSON)绝不能拖垮已经安全的 agent,故 **fail-open**(默认 accept 放草稿过)。踩坑:我口头选了 fail-open,但 `try` 只包了解析、API 调用在外面 → **模型宕机时 create 抛异常没接住 = 实际 fail-closed(崩溃式挡死)**,和我的声明打架。改成整段 try 才自洽。并用一条测试(judge 强制抛异常→断言仍 accept)**把 fail-open 钉死防回退**。生产还要 `logger.warning` 让「质检静默失灵」可观测(L5「验收看动作不看话术」同源)。
- **⭐代价不对称 → 从严**:误判 revise 只多一次重写(有 `MAX_REFLECTIONS=1` 兜底);误判 accept 是**坏话发给用户、收不回**。代价不对称时 rubric 偏「存疑从严」。
- **落点选择本身是设计**:反思只挂在「正常给用户的话」那个出口;**转人工(terminal)在工具循环里就提前 return,天然进不了反思**——转人工已交人工,不该再自审。
- **必须有上限防 thrashing**:反思→改→再反思会 thrashing 甚至死循环(**L6 压缩阈值同类坑**),`MAX_REFLECTIONS=1`。
- **Evaluator 独立性**:同一模型自判有盲区(错答就是它写的);缓解=换视角提问(「站在合规审查角度…」)/换更强模型/代码红线兜底。judge 是「低频、质量关键」的活,可上更强模型(deepseek-v4-pro)。
- **可测性延续 L10**:reflect 的 `client` **依赖注入**→ 测试传假 client(SimpleNamespace 拼出 `.chat.completions.create().choices[0].message.content`)、零网络「拔网线还能过」。一句话:**网络调用是副作用,作为依赖注入而非内部构造,函数才从不可测变可测。**

## 十二、Multi-Agent 多智能体协作(新增,对标书 Ch10,JD1/3 点名)

- **"什么时候该上多 Agent、什么时候单 Agent 就够"(核心判据,王牌)**:唯一硬判据=**协作有没有引入单 Agent 生成时拿不到的新信息**。引入了(代码执行反馈/视觉截图/工具验证事实)→ 显著提升(RLEF/WebGen-Agent);没引入(同模型自审、纯辩论看同一段文本)→ 通常无效甚至有害。**成本**:Anthropic 多 Agent 研究系统 ≈ 15× token,收益必须盖得住。→ 判据一句话:**别为"多几个 agent"付 15 倍的钱,除非它带来了新信息。**
- **两个正交设计维度(架构选型框架)**:① 上下文**共享 vs 隔离**(= 线程 vs 进程:共享=继承完整轨迹、无隔离难并行;隔离=只看显式传的、可安全并行但总 token 高数倍) ② 协作**拓扑**(对等 Peer 迭代改进 / 管理者 Orchestration 中心调度 / 去中心化 handoff)。选择依据:上下文>窗口50%→隔离;信息零损耗硬约束→共享;多数系统"阶段切换式"。通信=IPC 两范式(共享文件系统=共享内存 / 工具参数·消息总线=消息传递)。
- **⭐打脸呼应(证明判据内化了)**:我上节做的 reflect = **同模型自我审查** = 判据表里"通常无效"那行——这解释了它端到端全 accept。**我的"该不该每轮反思"工程直觉,被 Ch10 判据从理论上印证。**
- **⭐动手项目=多 Agent 代码审查系统(自己提的,踩对判据)**:管理者模式+隔离上下文+并行。**刻意让 reviewer 带真实工具反馈(pyflakes 抓未定义名/未用 import)而非堆同模型 LLM lens**——因为 N 个同模型 reviewer 读同一段 diff 不引入新信息。**活证据:pyflakes 抓到 `db` 未定义/`os` 未用(LLM 漏),LLM 抓到 SQL 注入/除零(工具看不见)——价值来自异质性不是人头数。**
- **隔离为什么重要(三点,面试话术)**:① 省 token(不继承膨胀) ② **防 anchoring**——串在一段历史里后面的 reviewer 会附和前面、意见变相关→退化成"看同一段文本互相点头"=不引入新信息 ③ 解锁并行(互不依赖才能同时跑)。**隔离是"多视角"能成立的前提。**
- **并行选型(面试常问)**:I/O-bound(等 LLM/等 subprocess)用 **ThreadPoolExecutor**(等 I/O 时释放 GIL,真重叠);CPU-bound 用 ProcessPool。写法:**先全 submit 再收 result**(否则退化串行);`list.append` 在 GIL 下原子,共享简单结构不用自己加锁。
- **§4 防级联错误**:汇总别无脑拼接——**工具证据(确定性)排 LLM 意见(概率)前面**,别让一堆 LLM 意见淹没确定事实。去重是字符串级`(line,message)`,语义重复(同 bug 不同措辞)合不掉,要合得上 embedding/LLM=加成本(诚实局限)。
- **本节工程坑(真实故事)**:① subprocess 用 `sys.executable` 不用裸 python(哪个 python 坑) ② **解析工具输出:已知的东西(路径)先剥掉再解析未知部分**(Windows 盘符冒号打乱 split) ③ **prompt↔parser 是同一契约的两处,必须同步**(改一半→findings 丢光);source 由代码盖章不问模型 ④ 测试断言要对着**工具真实输出**(pyflakes 说英文)不是"我以为"。

## 待补(学完对应课程后回来填)
- [x] ~~L6 遗留:单元测试系统学习~~ → **已还**:capstone 亲手写 9 条(tools 6 + policy 3),并系统学 mock/monkeypatch/惰性化可测性(见上「mock 深水区四连招」)
- [x] ~~L9:LangChain 实战~~ → **已填**(见「九、L9」)
- [x] ~~L10:MCP~~ → **已填**(见「十、L10」)
- [x] ~~L11:Multi-Agent~~ → **已填**(见「十二、Multi-Agent」)
- [ ] L12/L13:评估指标与可观测性
- [ ] L14:部署
